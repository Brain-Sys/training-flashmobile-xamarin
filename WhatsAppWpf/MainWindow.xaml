<Window x:Class="WhatsAppWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhatsAppWpf"
        xmlns:viewmodels="clr-namespace:WhatsAppWpf.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">

    <Window.Resources>
        <viewmodels:MainViewModel x:Key="vm" />
    </Window.Resources>
    
    <StackPanel DataContext="{StaticResource vm}">
        <Button Command="{Binding RegisterNewDeviceCommand}" Content="REGISTER" Margin=" 16 8" />
        <Button Command="{Binding TestCommand}" Content="TEST" Margin=" 16 8" />
        <Button Command="{Binding CreateChatCommand}" Content="CREATE CHAT" Margin=" 16 8" />
        <Button Command="{Binding DeleteChatCommand}" Content="DELETE CHAT" Margin=" 16 8" />
    </StackPanel>
</Window>